channels:
  
  - conda-forge

dependencies:

  - python=3.8.7

  - pip=20.1
  - pip:
      # Define pip packages here.
      # https://pypi.org/
      - rpaframework==12.0.0 # https://rpaframework.org/releasenotes.html
